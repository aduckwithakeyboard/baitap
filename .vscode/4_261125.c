#include <stdio.h>
void main()
{
    char A[100];
    int count=0;
    printf("nhap chuoi:");gets(A);
    while(A[count]!='\0')
    {
        count++;
    }
    for(int i=count;i>=n 0;i--)
        printf("%c",A[i]);
}