#include <stdio.h>
int main(){
    char c;
    
    // Using %c for character input
    scanf("%c", &c);

    // Using %c for character output
    printf("The entered character: %c", c);
    return 0;
}